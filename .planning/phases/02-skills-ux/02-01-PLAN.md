---
phase: 02-skills-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills-global/id3-start/references/phase-guide.md
  - skills-global/id3-start/references/dashboard-template.md
  - skills-global/id3-start/SKILL.md
autonomous: true
requirements: [ROUT-01, ROUT-02, ROUT-03, ROUT-04, ROUT-05, ROUT-06, ROUT-07, ROUT-08, ROUT-09, ROUT-10, UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "User runs /id3-start in a fresh project and IDDD files are auto-setup before routing"
    - "User sees progress dashboard with Phase pipeline boxes, progress bar, and status message"
    - "User runs /id3-start [request] and gets routed to the correct IDDD phase skill"
    - "User requests a pure UI task and gets routed directly to id3-design-ui"
    - "User runs /id3-start without arguments and sees project status with suggested next action"
    - "Ambiguous requests trigger a clarification question"
    - "Routing announcement includes phase description and artifact list"
  artifacts:
    - path: "skills-global/id3-start/SKILL.md"
      provides: "Smart router orchestration logic"
      contains: "name: id3-start"
    - path: "skills-global/id3-start/references/phase-guide.md"
      provides: "Routing taxonomy with per-phase signals, conditions, descriptions, artifacts"
      contains: "id3-identify-entities"
    - path: "skills-global/id3-start/references/dashboard-template.md"
      provides: "ASCII dashboard template with rendering instructions"
      contains: "Phase 0/1"
  key_links:
    - from: "skills-global/id3-start/SKILL.md"
      to: "references/phase-guide.md"
      via: "markdown link reference"
      pattern: "phase-guide\\.md"
    - from: "skills-global/id3-start/SKILL.md"
      to: "references/dashboard-template.md"
      via: "markdown link reference"
      pattern: "dashboard-template\\.md"
    - from: "skills-global/id3-start/SKILL.md"
      to: "npx id3-cli ."
      via: "Bash tool instruction for auto-setup"
      pattern: "npx id3-cli"
---

<objective>
Replace the placeholder id3-start SKILL.md with a full smart router implementation including reference files for the phase guide and dashboard template.

Purpose: This is the core deliverable of Phase 2 -- the /id3-start slash command that auto-detects IDDD installation, shows a progress dashboard, analyzes user intent, and routes to the correct IDDD phase skill.
Output: 3 files -- SKILL.md (~300 lines), references/phase-guide.md (~120 lines), references/dashboard-template.md (~80 lines)
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-skills-ux/02-CONTEXT.md
@.planning/phases/02-skills-ux/02-RESEARCH.md
@.planning/phases/02-skills-ux/02-UI-SPEC.md

<interfaces>
<!-- Existing placeholder to replace -->
From skills-global/id3-start/SKILL.md (current):
```yaml
---
name: id3-start
description: >
  IDDD smart entry point. Analyzes your request and routes to the right
  phase. Auto-sets up IDDD if not initialized. Use for any IDDD work.
user-invocable: true
allowed-tools: Read Glob Grep Bash Write Edit
---
```

<!-- Routing target skill names (from templates/skills/) -->
- /id3-identify-entities -- Phase 0/1 Entities
- /id3-design-information -- Phase 2 Info Model
- /id3-design-ui -- Phase 2.5 UI Design
- /id3-spawn-team -- Phase 3-5 Build
- /id3-info-audit -- Audit
- /id3-preview -- Preview

<!-- YAML frontmatter schema from specs templates -->
From templates/specs/entity-catalog.md:
```yaml
---
version: "0.0"
last_verified: ""
phase: "Initialized"
entity_count: 0
rule_count: 0
audit_status: "unverified"
---
```

From templates/specs/ui-structure.md:
```yaml
---
version: "0.0"
derived_from: ""
screen_count: 0
entity_coverage: ""
---
```

<!-- Banner pattern from assets/banner.txt -->
Uses double-line box characters: top-left-double, vertical-double, bottom-left-double, etc.
The banner is 88 chars wide with the IDDD block letter logo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reference files (phase-guide.md + dashboard-template.md)</name>
  <files>skills-global/id3-start/references/phase-guide.md, skills-global/id3-start/references/dashboard-template.md</files>
  <read_first>
    - templates/skills/id3-identify-entities/SKILL.md (routing target: trigger keywords, prerequisites)
    - templates/skills/id3-design-information/SKILL.md (routing target: trigger keywords, prerequisites)
    - templates/skills/id3-design-ui/SKILL.md (routing target: trigger keywords, prerequisites)
    - templates/skills/id3-spawn-team/SKILL.md (routing target: trigger keywords, prerequisites)
    - templates/skills/id3-info-audit/SKILL.md (routing target: trigger keywords)
    - templates/skills/id3-preview/SKILL.md (routing target: trigger keywords)
    - templates/specs/entity-catalog.md (YAML frontmatter schema for state detection)
    - templates/specs/data-model.md (YAML frontmatter schema for state detection)
    - templates/specs/ui-structure.md (YAML frontmatter schema for state detection)
    - .planning/phases/02-skills-ux/02-CONTEXT.md (dashboard format decision, routing rules)
    - .planning/phases/02-skills-ux/02-UI-SPEC.md (ASCII component specs, copywriting contract)
    - assets/banner.txt (banner style reference for dashboard header)
  </read_first>
  <action>
Create the `skills-global/id3-start/references/` directory and write two reference files.

**File 1: `references/phase-guide.md` (~120 lines)**

This file contains the routing taxonomy used by the SKILL.md router. Structure:

```markdown
# IDDD Phase Guide

Reference for the id3-start router. Each phase lists signal keywords, routing conditions,
the target skill name, a one-line description, and produced artifacts.

## Phase 0/1: Entity Identification
- **Target skill:** /id3-identify-entities
- **Signal keywords:** new project, domain analysis, entity identification, information analysis, what entities, identify entities, analyze domain
- **Condition:** No `specs/entity-catalog.md` exists, OR entity-catalog version is "0.0", OR user explicitly requests entity work
- **Description:** Identify domain entities from existing code (brownfield) or through structured interview (greenfield)
- **Artifacts produced:** specs/entity-catalog.md (with entities, attributes, relationships)

## Phase 2: Information Design
- **Target skill:** /id3-design-information
- **Signal keywords:** data model, information design, relationships, attributes, design information, refine model, logical model, schema design
- **Condition:** entity-catalog exists with version >= "0.1", user requests model refinement
- **Description:** Refine conceptual model into logical model with business rules, constraints, and derivation rules
- **Artifacts produced:** specs/data-model.md (ERD + relationships), docs/business-rules.md

## Phase 2.5: UI Design
- **Target skill:** /id3-design-ui
- **Signal keywords:** UI design, screen design, frontend, layout, wireframe, design ui, implement ui, screens
- **Condition:** data-model exists with version >= "1.0", user requests UI work
- **Fast-path (ROUT-05):** If the user's request contains ONLY explicit UI keywords (e.g., "design login screen", "create dashboard layout") AND a data-model already exists, route here directly -- skip the entity identification question
- **Description:** Design and implement UI derived from the information model
- **Artifacts produced:** specs/ui-structure.md, specs/ui-design-contract.md

## Phase 3-5: Build (Agent Teams)
- **Target skill:** /id3-spawn-team
- **Signal keywords:** build, implement, code, develop, spawn team, start implementation, parallel implementation
- **Condition:** ui-structure exists with version > "0.0", user requests implementation
- **Description:** Spawn Agent Teams for parallel spec generation, implementation, and QA review
- **Artifacts produced:** Implementation code, tests, API endpoints

## Audit
- **Target skill:** /id3-info-audit
- **Signal keywords:** audit, verify, check model, drift, consistency check, run audit
- **Condition:** User requests audit at any point (no prerequisites)
- **Description:** Compare information model against actual codebase for drift detection
- **Artifacts produced:** Audit report in .iddd/preview/

## Preview
- **Target skill:** /id3-preview
- **Signal keywords:** preview, visualize, show model, view ERD, show diagram, render
- **Condition:** User requests preview at any point (no prerequisites)
- **Description:** Generate HTML renderings of ERD, UI mockups, audit reports
- **Artifacts produced:** HTML files in .iddd/preview/

## Ambiguous Request Handling (ROUT-06)

If the user's request could match BOTH UI-related AND data/entity-related routes (e.g., "add filter to the list", "add a search feature", "update the user profile page"), ask:

"Should this be a UI-only change, or does it need new data/entities? Please clarify."

Examples of ambiguous requests:
- "add filter to list" -- could be UI filter widget OR new filter entity/attribute
- "add search" -- could be UI search bar OR search indexing infrastructure
- "update user profile" -- could be UI layout change OR new user attributes
```

**File 2: `references/dashboard-template.md` (~80 lines)**

This file contains the exact ASCII dashboard template and rendering instructions. Structure:

```markdown
# Dashboard Template

Reproduce this template EXACTLY, replacing only the placeholder values
with actual data from the project state. Do NOT reformat, reword,
or "improve" the layout. Use the exact box-drawing characters shown.

## Header

(Use double-line box characters matching the project's banner.txt style)

(The template below is shown with Unicode character names for clarity.
Use the actual Unicode characters when rendering.)

The header is a double-line box, 60 characters inner width:

Top border: one top-left-double-corner, then 60 repeated double-horizontal, then top-right-double-corner
Line 2: vertical-double + "  IDDD - Information Design-Driven Development" padded to 60 chars + vertical-double
Line 3: vertical-double + "  Your information model is your harness." padded to 60 chars + vertical-double
Bottom border: one bottom-left-double-corner, then 60 repeated double-horizontal, then bottom-right-double-corner

## Phase Pipeline

Four boxes connected by arrows. Each box is 16 characters wide (14 inner + 2 single-line borders).
Boxes are connected by "───>" (4-char gutter).

Line 1 (top borders):   4 top borders separated by 4 spaces
Line 2 (phase labels):  "  Phase 0/1   ", "   Phase 2    ", "  Phase 2.5   ", "  Phase 3-5   "
Line 3 (phase names):   "  Entities    ", "  Info Model  ", "  UI Design   ", "    Build     "
Line 4 (status):        "    {symbol}  {value}    " -- see Status Symbols below
Line 5 (bottom borders): 4 bottom borders separated by 4 spaces

Between each pair of boxes on lines 2-4, show "───>" connector.

### Status Symbols and Values

For each phase box, determine status from project state:

| Phase | Check | Complete Symbol + Value | In-Progress | Not Started |
|-------|-------|------------------------|-------------|-------------|
| Phase 0/1 | entity-catalog version | checkmark + entity_count (e.g., "12") | diamond | circle |
| Phase 2 | entity-catalog version >= "1.0" | checkmark + "v" + version (e.g., "v1.0") | diamond | circle |
| Phase 2.5 | ui-structure exists with version > "0.0" | checkmark + screen_count | diamond | circle |
| Phase 3-5 | (manual check -- no automated indicator) | checkmark | diamond | circle |

Phase completion mapping:
- entity-catalog version "0.0" or missing = Phase 0/1 not started
- entity-catalog version "0.1" = Phase 0/1 complete, Phase 2 in-progress
- entity-catalog version "1.0"+ = Phase 2 complete
- ui-structure exists with version > "0.0" = Phase 2.5 complete
- Build status: mark as diamond if Phase 2.5 is complete, circle otherwise

Only ONE phase may show diamond (the currently actionable phase). All phases after it show circle.

## Progress Bar

Total width: 66 characters for the bar + 4 for the percentage label.
Top rule: 70 repeated single-horizontal
Bar: filled-blocks (for completed %) + light-shade blocks (for remaining %) + "  NN%"
Bottom rule: 70 repeated single-horizontal

Progress calculation (equal weight per phase):
- Entities (Phase 0/1): 25%
- Info Model (Phase 2): 25%
- UI Design (Phase 2.5): 25%
- Build (Phase 3-5): 25%

Each completed phase adds its weight. In-progress phase adds 0%.

## Status Message

Two lines prefixed with "> ":

Line 1: "> " + status description based on current state
Line 2 (if $ARGUMENTS provided): "> Request: \"{user's request}\""
Line 2 (if no $ARGUMENTS): "> Suggested next action: {next phase name}. Use `/id3-start [your request]` to begin."

Status descriptions by state:
- No phases complete: "Current status: Ready to begin entity identification"
- Phase 0/1 complete: "Current status: Entity identification complete. Ready for information design"
- Phase 2 complete: "Current status: Information model complete. Ready for UI design"
- Phase 2.5 complete: "Current status: UI design complete. Ready for implementation"
- All complete: "Current status: All phases complete. Use /id3-info-audit for model verification"
```

Ensure both files use plain markdown with no YAML frontmatter (they are reference files, not skills).
  </action>
  <verify>
    <automated>test -f skills-global/id3-start/references/phase-guide.md && test -f skills-global/id3-start/references/dashboard-template.md && grep -q "id3-identify-entities" skills-global/id3-start/references/phase-guide.md && grep -q "id3-design-ui" skills-global/id3-start/references/phase-guide.md && grep -q "Phase 0/1" skills-global/id3-start/references/dashboard-template.md && grep -q "Progress" skills-global/id3-start/references/dashboard-template.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - phase-guide.md contains "## Phase 0/1: Entity Identification" section
    - phase-guide.md contains "## Phase 2: Information Design" section
    - phase-guide.md contains "## Phase 2.5: UI Design" section
    - phase-guide.md contains "## Phase 3-5: Build" section
    - phase-guide.md contains "## Audit" section
    - phase-guide.md contains "## Preview" section
    - phase-guide.md contains "## Ambiguous Request Handling" section
    - phase-guide.md contains "/id3-identify-entities", "/id3-design-information", "/id3-design-ui", "/id3-spawn-team", "/id3-info-audit", "/id3-preview"
    - phase-guide.md contains "Fast-path" under Phase 2.5 section
    - dashboard-template.md contains "## Header" with double-line box instructions
    - dashboard-template.md contains "## Phase Pipeline" with 4-box layout
    - dashboard-template.md contains "## Progress Bar" with 25% weight per phase
    - dashboard-template.md contains "## Status Message" with "> " prefix format
    - dashboard-template.md contains status symbol mapping table (checkmark, diamond, circle)
    - dashboard-template.md contains "Reproduce this template EXACTLY"
    - Neither file contains YAML frontmatter (--- blocks)
  </acceptance_criteria>
  <done>Both reference files exist with complete routing taxonomy and dashboard template matching CONTEXT.md and UI-SPEC.md specifications</done>
</task>

<task type="auto">
  <name>Task 2: Replace id3-start placeholder with full smart router SKILL.md</name>
  <files>skills-global/id3-start/SKILL.md</files>
  <read_first>
    - skills-global/id3-start/SKILL.md (current placeholder to replace)
    - skills-global/id3-start/references/phase-guide.md (just created in Task 1)
    - skills-global/id3-start/references/dashboard-template.md (just created in Task 1)
    - .planning/phases/02-skills-ux/02-CONTEXT.md (locked decisions: routing rules, auto-setup UX, dashboard format)
    - .planning/phases/02-skills-ux/02-RESEARCH.md (SKILL.md structure, patterns, pitfalls)
    - .planning/phases/02-skills-ux/02-UI-SPEC.md (interaction flows, copywriting contract, component specs)
    - assets/banner.txt (welcome banner pattern for first-time setup)
    - templates/specs/entity-catalog.md (YAML frontmatter schema)
    - src/init.ts (initProject interface -- invoked via npx id3-cli .)
  </read_first>
  <action>
Replace the placeholder `skills-global/id3-start/SKILL.md` with the full smart router implementation (~250-300 lines). The file structure must follow this exact layout:

**YAML Frontmatter:**
```yaml
---
name: id3-start
description: >
  IDDD smart entry point. Analyzes your request and routes to the right
  IDDD phase. Auto-sets up IDDD if not initialized. Shows progress dashboard.
  Trigger: start IDDD, begin project, what should I do next, identify entities,
  design information, design ui, build, audit, preview
user-invocable: true
allowed-tools: Read Glob Grep Bash Write Edit
---
```

**Body structure (5 major sections):**

**Section: "# IDDD Smart Router"**
Opening paragraph: "You are the IDDD workflow entry point. Your job is to: (1) Ensure IDDD is set up in this project, (2) Show the project progress dashboard, (3) Analyze the user's request and route to the correct skill."

Then: "User request: $ARGUMENTS"

Then: "Communicate with the user in their language. If the user writes in Korean, respond in Korean. If English, respond in English."

**Section: "## Step 1: Check IDDD Installation"**
Instructions for Claude to check if IDDD is installed:
1. Use Glob tool to check if `specs/entity-catalog.md` exists AND `CLAUDE.md` exists in the project root
2. If BOTH exist: "IDDD is installed. Skip to Step 2."
3. If EITHER is missing:
   a. Check for project root indicators: `package.json`, `.git/`, `Cargo.toml`, `go.mod`, `pyproject.toml`, `Makefile`. If NONE found, show non-project directory warning: "This directory does not appear to be a project root (no package.json, .git, etc.). Are you sure you want to set up IDDD here? [y/N]" -- wait for user response before proceeding.
   b. Show welcome message: render the IDDD banner using double-line box characters matching the style in `assets/banner.txt`. Include the tagline "Your information model is your harness." inside the banner.
   c. Show auto-setup notice: "IDDD is not set up in this project. Setting up now..."
   d. Run auto-setup: Use Bash tool to execute `npx id3-cli .` in the current directory. Do NOT ask for user confirmation (per CONTEXT.md decision -- auto-run).
   e. Show completion: "IDDD initialized. Here is your project dashboard:"
   f. Continue to Step 2.

**Section: "## Step 2: Read Project State"**
Instructions for Claude to read YAML frontmatter from specs files:
1. Read `specs/entity-catalog.md` -- extract `version`, `entity_count`, `rule_count`, `phase` from YAML frontmatter between `---` markers
2. Read `specs/data-model.md` -- extract `version`, `rule_count` from YAML frontmatter
3. Check if `specs/ui-structure.md` exists -- if yes, read and extract `version`, `screen_count`
4. Map to phase completion status:
   - entity-catalog version "0.0" or missing = Phase 0/1 not started
   - entity-catalog version "0.1" = Phase 0/1 complete
   - entity-catalog version "1.0" or higher = Phase 2 complete
   - ui-structure exists with version > "0.0" = Phase 2.5 complete
   - Build status: if Phase 2.5 is complete, Build is in-progress (diamond); otherwise not-started (circle)
5. Determine the "current phase" = first incomplete phase in order

**Section: "## Step 3: Show Dashboard"**
"Render the progress dashboard following the template in [references/dashboard-template.md](references/dashboard-template.md) EXACTLY. Fill in the status symbols and values from the project state detected in Step 2. Do not reformat or improve the layout."

**Section: "## Step 4: Route or Suggest"**
Two branches:

"**If $ARGUMENTS is empty or contains only whitespace:**"
- Show suggested next action based on current phase state
- Format: "> Suggested next action: {phase name}. Use `/id3-start [your request]` to begin."
- Stop here. Do not attempt routing.

"**If $ARGUMENTS contains a request:**"
- Analyze the user's request against the routing taxonomy in [references/phase-guide.md](references/phase-guide.md)
- Present the current project state to the user and which Phase you recommend
- Show routing announcement: "Routing to {skill name} -- {phase one-liner description}."
- Show artifacts: "This phase produces: {artifact list from phase-guide}."
- Then invoke the target skill. Include the user's original request as context when delegating.

**Sub-section: "### Routing Decision Rules"**
1. If the request matches a single phase clearly, route there
2. If the request is UI-only AND data-model exists (version >= "1.0"), fast-path to /id3-design-ui (ROUT-05)
3. If the request is ambiguous (could be UI-only or needs new data), ask: "Should this be a UI-only change, or does it need new data/entities? Please clarify." (ROUT-06)
4. If the target phase has prerequisites not met (e.g., routing to Phase 2 but Phase 0/1 not done), inform the user: "Phase {N} requires {prerequisite}. Please complete {earlier phase} first." Then suggest the correct starting phase. (ROUT-08). Note: each target skill also has its own built-in prerequisite checks, so the router's check is a courtesy -- the target skill will enforce regardless.
5. After routing decision is confirmed, invoke the target skill using its slash command name (e.g., use /id3-identify-entities). Pass the user's original request as conversational context. (ROUT-07, ROUT-09)

**Footer note:**
"Note: This skill is the global entry point. The 6 phase skills (id3-identify-entities, id3-design-information, id3-design-ui, id3-spawn-team, id3-info-audit, id3-preview) are project-scoped and installed by `npx id3-cli .` into the project's skills directory."

Keep the total SKILL.md under 300 lines. Move detailed routing rules and dashboard rendering to the reference files -- do not duplicate them inline.
  </action>
  <verify>
    <automated>grep -c "^##" skills-global/id3-start/SKILL.md | xargs -I{} test {} -ge 4 && grep -q "name: id3-start" skills-global/id3-start/SKILL.md && grep -q "ARGUMENTS" skills-global/id3-start/SKILL.md && grep -q "npx id3-cli" skills-global/id3-start/SKILL.md && grep -q "phase-guide.md" skills-global/id3-start/SKILL.md && grep -q "dashboard-template.md" skills-global/id3-start/SKILL.md && grep -q "entity-catalog" skills-global/id3-start/SKILL.md && wc -l < skills-global/id3-start/SKILL.md | xargs -I{} test {} -le 350 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md frontmatter contains "name: id3-start"
    - SKILL.md frontmatter contains "user-invocable: true"
    - SKILL.md frontmatter contains "allowed-tools: Read Glob Grep Bash Write Edit"
    - SKILL.md contains "$ARGUMENTS" (user input variable)
    - SKILL.md contains "## Step 1: Check IDDD Installation" with Glob check for specs/entity-catalog.md and CLAUDE.md
    - SKILL.md contains "npx id3-cli ." instruction for auto-setup (ROUT-01)
    - SKILL.md contains project root indicator check (package.json, .git) before auto-setup
    - SKILL.md contains idempotency logic: "If BOTH exist" skip setup (ROUT-10)
    - SKILL.md contains "## Step 2: Read Project State" with YAML frontmatter reading instructions (ROUT-03)
    - SKILL.md contains version-to-phase mapping: "0.0" = not started, "0.1" = Phase 0/1 done, "1.0" = Phase 2 done (ROUT-03)
    - SKILL.md contains "## Step 3: Show Dashboard" referencing dashboard-template.md (UX-01)
    - SKILL.md contains "## Step 4: Route or Suggest" with empty/non-empty $ARGUMENTS branching (UX-02)
    - SKILL.md contains suggested next action text for no-args case (UX-02)
    - SKILL.md contains routing announcement format with phase description and artifacts (ROUT-04, UX-03)
    - SKILL.md contains fast-path rule for UI-only requests to /id3-design-ui (ROUT-05)
    - SKILL.md contains ambiguous request clarification question (ROUT-06)
    - SKILL.md contains target skill invocation instruction (ROUT-07)
    - SKILL.md contains prerequisite check with guidance to correct starting point (ROUT-08)
    - SKILL.md contains instruction to pass user request as context to target skill (ROUT-09)
    - SKILL.md references phase-guide.md for detailed routing taxonomy
    - SKILL.md references dashboard-template.md for dashboard rendering
    - SKILL.md contains language adaptation instruction (respond in user's language)
    - SKILL.md is under 350 lines total
    - SKILL.md does NOT contain "Placeholder" text
  </acceptance_criteria>
  <done>id3-start SKILL.md is a complete smart router with auto-setup, dashboard, intent classification, and routing -- replacing the placeholder entirely. All 13 requirements (ROUT-01 through ROUT-10, UX-01 through UX-03) are addressed in the skill content.</done>
</task>

</tasks>

<verification>
1. `skills-global/id3-start/SKILL.md` exists and is under 350 lines
2. `skills-global/id3-start/references/phase-guide.md` exists with all 6 routing targets
3. `skills-global/id3-start/references/dashboard-template.md` exists with pipeline + progress bar template
4. SKILL.md references both reference files
5. All ROUT-01 through ROUT-10 requirements addressed in SKILL.md content
6. All UX-01 through UX-03 requirements addressed in SKILL.md content
7. No TypeScript code created (pure SKILL.md approach per research recommendation)
</verification>

<success_criteria>
- The id3-start skill directory contains SKILL.md + references/ with 2 files
- SKILL.md handles both fresh-project auto-setup and existing-project routing
- Dashboard template produces the box-pipeline + progress-bar layout from CONTEXT.md
- Routing taxonomy covers all 6 target skills with signal keywords and conditions
- Empty $ARGUMENTS shows status + suggestion; non-empty $ARGUMENTS routes to skill
- Ambiguous requests trigger clarification question
- UI-only requests fast-path to id3-design-ui
</success_criteria>

<output>
After completion, create `.planning/phases/02-skills-ux/02-01-SUMMARY.md`
</output>
