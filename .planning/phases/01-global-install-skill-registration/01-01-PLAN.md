---
phase: 01-global-install-skill-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills-global/id3-start/SKILL.md
  - skills-global/id3-clear/SKILL.md
  - src/register-skills.ts
  - tests/register-skills.test.ts
autonomous: true
requirements: [INST-02, INST-03, INST-05, INST-06]
must_haves:
  truths:
    - "registerSkills() copies id3-start and id3-clear SKILL.md files from skills-global/ to ~/.claude/skills/"
    - "unregisterSkills() removes id3-start and id3-clear directories from ~/.claude/skills/"
    - "getSkillsTargetDir() returns the correct path on macOS, Linux, and Windows using os.homedir()"
    - "registerSkills() is idempotent (can run multiple times without error)"
    - "unregisterSkills() is safe to call when skills do not exist"
  artifacts:
    - path: "skills-global/id3-start/SKILL.md"
      provides: "Placeholder skill file for id3-start slash command"
      contains: "name: id3-start"
    - path: "skills-global/id3-clear/SKILL.md"
      provides: "Placeholder skill file for id3-clear slash command"
      contains: "name: id3-clear"
    - path: "src/register-skills.ts"
      provides: "Skill registration and unregistration logic"
      exports: ["registerSkills", "unregisterSkills", "getSkillsTargetDir", "getSkillsSourceDir", "GLOBAL_SKILLS"]
    - path: "tests/register-skills.test.ts"
      provides: "Unit tests for skill registration"
      min_lines: 50
  key_links:
    - from: "src/register-skills.ts"
      to: "skills-global/"
      via: "getSkillsSourceDir() resolves to skills-global/ relative to compiled dist/src/ location"
      pattern: "join.*import\\.meta\\.dirname.*skills-global"
    - from: "src/register-skills.ts"
      to: "~/.claude/skills/"
      via: "getSkillsTargetDir() using os.homedir()"
      pattern: "homedir.*\\.claude.*skills"
    - from: "src/register-skills.ts"
      to: "src/utils/fs.ts"
      via: "imports copyDir and fileExists"
      pattern: "import.*copyDir.*fileExists.*utils/fs"
---

<objective>
Create the skill registration foundation: placeholder SKILL.md files for id3-start and id3-clear, the registration/unregistration module, and comprehensive tests.

Purpose: This is the core building block for Phase 1. All other plans depend on this module existing and being tested.
Output: `skills-global/` directory with two placeholder SKILL.md files, `src/register-skills.ts` with exported functions, passing test suite.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-global-install-skill-registration/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create placeholder SKILL.md files and register-skills module with tests</name>
  <files>skills-global/id3-start/SKILL.md, skills-global/id3-clear/SKILL.md, src/register-skills.ts, tests/register-skills.test.ts</files>
  <read_first>
    - src/utils/fs.ts (reuse copyDir and fileExists)
    - src/init.ts (see existing import.meta.dirname pattern for path resolution)
    - tests/init.test.ts (see existing test patterns: mkdtemp, cleanup, assertion style)
    - package.json (verify project structure)
  </read_first>
  <behavior>
    - Test: registerSkills() copies skills-global/id3-start/SKILL.md to {tempDir}/.claude/skills/id3-start/SKILL.md
    - Test: registerSkills() copies skills-global/id3-clear/SKILL.md to {tempDir}/.claude/skills/id3-clear/SKILL.md
    - Test: unregisterSkills() removes {tempDir}/.claude/skills/id3-start/ directory
    - Test: unregisterSkills() removes {tempDir}/.claude/skills/id3-clear/ directory
    - Test: unregisterSkills() does not throw when skills directories do not exist
    - Test: registerSkills() creates intermediate directories (~/.claude/skills/) if they do not exist
    - Test: registerSkills() overwrites existing SKILL.md files (idempotent)
    - Test: getSkillsTargetDir() returns path ending in .claude/skills (cross-platform via os.homedir())
    - Test: getSkillsSourceDir() returns path ending in skills-global
  </behavior>
  <action>
    **Step 1: Create placeholder SKILL.md files**

    Create `skills-global/id3-start/SKILL.md`:
    ```yaml
    ---
    name: id3-start
    description: >
      Smart entry point for IDDD workflow. Analyzes your request and routes to
      the right IDDD phase. Use for starting work, guided IDDD workflow, or
      when unsure which phase to use.
      Trigger: start project, begin development, what should I do next
    user-invocable: true
    allowed-tools: Read Glob Grep Bash Write Edit
    ---

    # id3-start

    This skill is a placeholder. Full implementation comes in Phase 2.

    Use $ARGUMENTS to see user input.
    ```

    Create `skills-global/id3-clear/SKILL.md`:
    ```yaml
    ---
    name: id3-clear
    description: >
      Reset IDDD project files. Removes all IDDD-generated files and folders
      from the current project.
      Trigger: clear project, reset iddd, remove iddd files, clean up
    user-invocable: true
    allowed-tools: Read Glob Grep Bash Write Edit
    ---

    # id3-clear

    This skill is a placeholder. Full implementation comes in Phase 2.

    Use $ARGUMENTS to see user input.
    ```

    **Step 2: Create `src/register-skills.ts`**

    The module exports:
    - `GLOBAL_SKILLS: string[]` = `['id3-start', 'id3-clear']`
    - `getSkillsTargetDir(): string` - returns `join(homedir(), '.claude', 'skills')` using `os.homedir()` from `node:os`
    - `getSkillsSourceDir(): string` - returns `join(import.meta.dirname, '..', '..', 'skills-global')` (from dist/src/ up 2 levels to package root)
    - `registerSkills(): Promise<void>` - for each skill in GLOBAL_SKILLS, calls `copyDir(join(sourceDir, skill), join(targetDir, skill), { overwrite: true })`. Import `copyDir` from `./utils/fs.js`.
    - `unregisterSkills(): Promise<void>` - for each skill in GLOBAL_SKILLS, checks `fileExists(destDir)` and if true calls `rm(destDir, { recursive: true })`. Import `rm` from `node:fs/promises`. Import `fileExists` from `./utils/fs.js`.

    All path construction uses `path.join()` (never string concatenation with `/`).

    **Step 3: Write tests in `tests/register-skills.test.ts`**

    Follow existing test patterns from `tests/init.test.ts`:
    - Use `mkdtemp` for temp directories in `beforeEach`
    - Clean up with `rm` in `afterEach`
    - Use `vi.mock` or dependency injection to override `getSkillsTargetDir()` and `getSkillsSourceDir()` to point to temp directories

    To make the module testable without mocking `import.meta.dirname`, add optional parameters to `registerSkills` and `unregisterSkills`:
    ```typescript
    export async function registerSkills(options?: { sourceDir?: string; targetDir?: string }): Promise<void>
    export async function unregisterSkills(options?: { targetDir?: string }): Promise<void>
    ```
    When options are not provided, use the default `getSkillsSourceDir()` and `getSkillsTargetDir()`. Tests pass explicit temp directory paths.

    Test setup creates a fake `skills-global/` structure in a temp dir:
    ```typescript
    // In beforeEach:
    sourceDir = await mkdtemp(join(tmpdir(), 'iddd-src-'));
    targetDir = await mkdtemp(join(tmpdir(), 'iddd-tgt-'));
    await mkdir(join(sourceDir, 'id3-start'), { recursive: true });
    await mkdir(join(sourceDir, 'id3-clear'), { recursive: true });
    await writeFile(join(sourceDir, 'id3-start', 'SKILL.md'), '---\nname: id3-start\n---');
    await writeFile(join(sourceDir, 'id3-clear', 'SKILL.md'), '---\nname: id3-clear\n---');
    ```

    Each test calls `registerSkills({ sourceDir, targetDir })` or `unregisterSkills({ targetDir })` and asserts file existence/content/absence.
  </action>
  <verify>
    <automated>cd /data/bruce/iddd && npx vitest run tests/register-skills.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `skills-global/id3-start/SKILL.md` exists and contains the string `name: id3-start`
    - File `skills-global/id3-clear/SKILL.md` exists and contains the string `name: id3-clear`
    - Both SKILL.md files contain `user-invocable: true`
    - File `src/register-skills.ts` exists
    - `src/register-skills.ts` contains `export const GLOBAL_SKILLS`
    - `src/register-skills.ts` contains `export function getSkillsTargetDir`
    - `src/register-skills.ts` contains `export function getSkillsSourceDir`
    - `src/register-skills.ts` contains `export async function registerSkills`
    - `src/register-skills.ts` contains `export async function unregisterSkills`
    - `src/register-skills.ts` imports from `'./utils/fs.js'`
    - `src/register-skills.ts` imports `homedir` from `'node:os'`
    - `src/register-skills.ts` imports `join` from `'node:path'`
    - File `tests/register-skills.test.ts` exists with at least 8 test cases (it/test blocks)
    - `npx vitest run tests/register-skills.test.ts` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>
    Two placeholder SKILL.md files exist in skills-global/ with correct frontmatter. register-skills.ts exports all 5 functions/constants. All unit tests pass covering registration, unregistration, idempotency, and missing-directory safety.
  </done>
</task>

</tasks>

<verification>
1. `ls skills-global/id3-start/SKILL.md skills-global/id3-clear/SKILL.md` -- both files exist
2. `grep -c "name: id3-start" skills-global/id3-start/SKILL.md` -- returns 1
3. `grep -c "name: id3-clear" skills-global/id3-clear/SKILL.md` -- returns 1
4. `grep -c "export" src/register-skills.ts` -- returns at least 5
5. `npx vitest run tests/register-skills.test.ts` -- all tests pass
6. `npx tsc --noEmit` -- TypeScript compiles without errors
</verification>

<success_criteria>
- skills-global/ directory contains two valid SKILL.md files with correct Claude Code frontmatter
- src/register-skills.ts exports registerSkills, unregisterSkills, getSkillsTargetDir, getSkillsSourceDir, GLOBAL_SKILLS
- All register-skills tests pass (registration, unregistration, cross-platform paths, idempotency, safety)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-global-install-skill-registration/01-01-SUMMARY.md`
</output>
