---
phase: 03-repo-package-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - .gitignore
autonomous: true
requirements:
  - REP-01
  - REP-02
  - REP-03
  - REP-04

must_haves:
  truths:
    - "LICENSE file exists at repository root with MIT text and correct copyright"
    - ".planning/ directory is not visible on GitHub (removed from git tracking)"
    - "tests/utils/ascii.test.ts and scripts/postinstall.ts are not visible on GitHub"
    - ".gitignore covers .planning/, editor files, OS artifacts, and all development artifacts"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: ".gitignore"
      provides: "Complete dev artifact exclusion"
      contains: ".planning/"
  key_links:
    - from: ".gitignore"
      to: "git index"
      via: "git rm --cached removes tracked files that .gitignore now covers"
      pattern: "git rm --cached"
---

<objective>
Clean the GitHub repository by adding the LICENSE file, updating .gitignore, and removing stale tracked files from git index.

Purpose: GitHub visitors should see a clean repo with proper licensing, no internal planning files, and no stale dev artifacts.
Output: LICENSE file created, .gitignore updated, .planning/ and stale test/script files removed from git tracking (files remain on disk).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/github-release.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSE file and update .gitignore</name>
  <files>LICENSE, .gitignore</files>
  <read_first>
    - .gitignore (current state to understand existing entries)
    - package.json (confirm license field is "MIT" and author name)
  </read_first>
  <action>
1. Create LICENSE file at repository root with standard MIT text:
   - Copyright line: "Copyright (c) 2026 Bruce Jung"
   - Use the full MIT license text (no modifications)
   - File name: LICENSE (no extension)

2. Update .gitignore to the following complete content:

```
# Dependencies
node_modules/

# Build output
dist/
*.tgz

# OS files
.DS_Store
Thumbs.db

# Lock files
package-lock.json

# Development artifacts - not for end users
docs/superpowers/
prd/
tests/
scripts/
vitest.config.ts
tsconfig.json

# AI agent / planning artifacts
.planning/
CLAUDE.md

# Editor artifacts
*.swp
*.swo
*~
.vscode/
.idea/
```

Key changes from current .gitignore:
- ADD: `.planning/` (36 tracked files to be untracked)
- ADD: `CLAUDE.md` (keep untracked for future)
- ADD: `Thumbs.db` (Windows OS artifact)
- ADD: `*.swp`, `*.swo`, `*~` (Vim swap files)
- ADD: `.vscode/`, `.idea/` (editor configs)
- KEEP: All existing entries unchanged
  </action>
  <acceptance_criteria>
    - File LICENSE exists at /data/bruce/iddd/LICENSE
    - grep "MIT License" LICENSE returns a match
    - grep "Copyright (c) 2026 Bruce Jung" LICENSE returns a match
    - grep "WITHOUT WARRANTY" LICENSE returns a match
    - grep ".planning/" .gitignore returns a match
    - grep "CLAUDE.md" .gitignore returns a match
    - grep "Thumbs.db" .gitignore returns a match
    - grep ".vscode/" .gitignore returns a match
    - grep ".idea/" .gitignore returns a match
    - grep "node_modules/" .gitignore returns a match (existing entry preserved)
    - grep "tests/" .gitignore returns a match (existing entry preserved)
  </acceptance_criteria>
  <verify>
    <automated>test -f LICENSE && grep -q "MIT License" LICENSE && grep -q ".planning/" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>LICENSE file exists with MIT text and Bruce Jung copyright. .gitignore covers .planning/, editor files, OS artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale tracked files from git index</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (must be updated from Task 1 before this runs)
  </read_first>
  <action>
Run the following git commands to remove files from tracking (keeps them on disk):

1. Remove .planning/ directory from git tracking:
   ```
   git rm --cached -r .planning/
   ```

2. Remove stale test and script files from git tracking:
   ```
   git rm --cached tests/utils/ascii.test.ts
   git rm --cached scripts/postinstall.ts
   ```

3. Stage the updated .gitignore and LICENSE file:
   ```
   git add .gitignore LICENSE
   ```

4. Commit all changes together with message:
   ```
   chore: add LICENSE, clean git tracking of dev artifacts

   - Add MIT LICENSE file
   - Update .gitignore to cover .planning/, editor files, OS artifacts
   - Remove .planning/ (36 files) from git tracking
   - Remove stale tests/utils/ascii.test.ts from git tracking
   - Remove stale scripts/postinstall.ts from git tracking
   ```

IMPORTANT: Use `git rm --cached` (not `git rm`) to keep files on disk. The --cached flag only removes from the git index.
  </action>
  <acceptance_criteria>
    - git ls-files .planning/ returns empty output (0 tracked files)
    - git ls-files tests/ returns empty output
    - git ls-files scripts/ returns empty output
    - git ls-files LICENSE returns "LICENSE" (file is tracked)
    - git ls-files .gitignore returns ".gitignore" (file is tracked)
    - ls .planning/STATE.md confirms file still exists on disk
    - git log --oneline -1 message contains "LICENSE" or "clean"
  </acceptance_criteria>
  <verify>
    <automated>test "$(git ls-files .planning/ | wc -l)" -eq 0 && test "$(git ls-files tests/ | wc -l)" -eq 0 && test "$(git ls-files scripts/ | wc -l)" -eq 0 && git ls-files LICENSE | grep -q LICENSE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero .planning/ files tracked in git. Zero tests/ and scripts/ files tracked. LICENSE is tracked. All removed files remain on disk.</done>
</task>

</tasks>

<verification>
- git ls-files .planning/ outputs nothing
- git ls-files tests/ outputs nothing
- git ls-files scripts/ outputs nothing
- test -f LICENSE confirms LICENSE exists
- grep "MIT License" LICENSE confirms MIT content
- grep ".planning/" .gitignore confirms gitignore coverage
- ls .planning/STATE.md confirms local files intact
</verification>

<success_criteria>
1. LICENSE file at repo root with MIT text and "Copyright (c) 2026 Bruce Jung"
2. .planning/ (36 files), tests/utils/ascii.test.ts, scripts/postinstall.ts removed from git tracking
3. All removed files still exist on disk
4. .gitignore covers .planning/, CLAUDE.md, editor files (.vscode/, .idea/), OS artifacts (Thumbs.db), and vim swap files
5. Changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/03-repo-package-cleanup/03-01-SUMMARY.md`
</output>
