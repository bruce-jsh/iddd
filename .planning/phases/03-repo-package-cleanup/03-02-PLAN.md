---
phase: 03-repo-package-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - skills-global/id3-start/SKILL.md
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - PKG-05

must_haves:
  truths:
    - "package.json contains repository, homepage, and bugs fields pointing to bruce-jsh/iddd"
    - "package.json engines field requires Node >=20.11.0"
    - "npm pack --dry-run does NOT include build-hooks.js, update-version.js, .d.ts files, or dist/templates/"
    - "prepublishOnly runs both build and test"
    - "id3-start/SKILL.md uses id3-cli init . instead of npx id3-cli ."
  artifacts:
    - path: "package.json"
      provides: "Complete npm package metadata and refined files field"
      contains: "repository"
    - path: "skills-global/id3-start/SKILL.md"
      provides: "SKILL.md with global install references"
  key_links:
    - from: "package.json files field"
      to: "npm pack output"
      via: "whitelist controls tarball contents"
      pattern: "dist/bin/.*\\.js"
    - from: "package.json prepublishOnly"
      to: "npm publish lifecycle"
      via: "runs build + test before every publish"
      pattern: "npm run build && npm test"
---

<objective>
Update package.json with production-ready metadata, refine the files field to exclude dev artifacts from the npm tarball, and fix npx references in SKILL.md.

Purpose: npm package should contain only runtime-necessary files, have complete metadata for the npmjs.com page, and reference the global install path instead of npx.
Output: package.json with repository/homepage/bugs/engines fields, refined files whitelist, prepublishOnly with test, and SKILL.md npx references fixed.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/npm-packaging.md
@.planning/research/pre-publish-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json metadata and files field</name>
  <files>package.json</files>
  <read_first>
    - package.json (current state — must see all existing fields to preserve them)
    - .planning/research/npm-packaging.md (recommended files field and metadata)
  </read_first>
  <action>
Update package.json with the following changes. Preserve ALL existing fields that are not listed here.

1. ADD these new fields (after "license" field):

```json
"repository": {
  "type": "git",
  "url": "https://github.com/bruce-jsh/iddd.git"
},
"homepage": "https://github.com/bruce-jsh/iddd#readme",
"bugs": {
  "url": "https://github.com/bruce-jsh/iddd/issues"
},
"engines": {
  "node": ">=20.11.0"
},
```

2. REPLACE the "files" field from:
```json
"files": ["dist/", "templates/", "assets/", "skills-global/"]
```
to:
```json
"files": [
  "dist/bin/**/*.js",
  "dist/src/**/*.js",
  "dist/scripts/postinstall.js",
  "templates/",
  "assets/",
  "skills-global/"
]
```

This change:
- Excludes dist/scripts/build-hooks.js (build tool, imports esbuild devDep)
- Excludes dist/scripts/update-version.js (version script, not runtime)
- Excludes dist/templates/hooks/ (duplicate of templates/hooks/)
- Excludes all .d.ts files (nobody imports from a CLI tool)
- Excludes all .js.map files (not needed for end users)
- Keeps dist/bin/**/*.js (CLI entry point)
- Keeps dist/src/**/*.js (runtime source)
- Keeps dist/scripts/postinstall.js (needed for npm postinstall hook)

3. REPLACE the "prepublishOnly" script from:
```json
"prepublishOnly": "npm run build"
```
to:
```json
"prepublishOnly": "npm run build && npm test"
```

Do NOT change: name, version, description, type, bin, other scripts, author, license, keywords, devDependencies.
  </action>
  <acceptance_criteria>
    - grep '"repository"' package.json returns a match
    - grep 'bruce-jsh/iddd.git' package.json returns a match
    - grep '"homepage"' package.json returns a match
    - grep 'bruce-jsh/iddd#readme' package.json returns a match
    - grep '"bugs"' package.json returns a match
    - grep 'bruce-jsh/iddd/issues' package.json returns a match
    - grep '"engines"' package.json returns a match
    - grep '>=20.11.0' package.json returns a match
    - grep 'dist/bin/\*\*/\*.js' package.json returns a match
    - grep 'dist/src/\*\*/\*.js' package.json returns a match
    - grep 'dist/scripts/postinstall.js' package.json returns a match
    - package.json does NOT contain the string "dist/" as a standalone entry (old broad glob)
    - grep 'npm run build && npm test' package.json returns a match
    - grep '"version": "0.9.3"' package.json returns a match (version NOT changed)
    - node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(p.name)" outputs "id3-cli"
    - npm pack --dry-run 2>&1 | grep build-hooks returns NO matches
    - npm pack --dry-run 2>&1 | grep update-version returns NO matches
    - npm pack --dry-run 2>&1 | grep '\.d\.ts' returns NO matches
    - npm pack --dry-run 2>&1 | grep 'dist/templates/' returns NO matches
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const checks=[p.repository?.url==='https://github.com/bruce-jsh/iddd.git', p.engines?.node==='>=20.11.0', p.scripts?.prepublishOnly==='npm run build && npm test', p.files?.includes('dist/bin/**/*.js'), !p.files?.includes('dist/')]; console.log(checks.every(Boolean)?'PASS':'FAIL: '+JSON.stringify(checks))"</automated>
  </verify>
  <done>package.json contains repository/homepage/bugs/engines fields. files field only includes runtime JS. prepublishOnly runs build and test.</done>
</task>

<task type="auto">
  <name>Task 2: Fix npx references in SKILL.md and verify npm pack</name>
  <files>skills-global/id3-start/SKILL.md</files>
  <read_first>
    - skills-global/id3-start/SKILL.md (current state, find exact npx lines)
    - .planning/research/pre-publish-checklist.md (npx removal details)
  </read_first>
  <action>
1. In skills-global/id3-start/SKILL.md, make these exact replacements:

   Line 69: Replace `npx id3-cli .` with `id3-cli init .`
   
   Line 188: Replace the text:
   "installed by `npx id3-cli .`"
   with:
   "installed by `id3-cli init .`"

2. After both files are saved (package.json from Task 1, SKILL.md from this task), run verification:

   ```bash
   # Build first (needed for npm pack to work with new files field)
   npm run build
   
   # Verify npm pack output
   npm pack --dry-run
   ```

   Confirm the dry-run output does NOT contain:
   - dist/scripts/build-hooks.js
   - dist/scripts/update-version.js
   - Any .d.ts files
   - dist/templates/ directory entries

   Confirm it DOES contain:
   - dist/bin/cli.js
   - dist/src/ files (*.js only)
   - dist/scripts/postinstall.js
   - templates/ directory
   - assets/ directory
   - skills-global/ directory

3. Commit both changes with message:
   ```
   chore: refine package.json for production release

   - Add repository, homepage, bugs, engines fields
   - Refine files field to exclude dev scripts, .d.ts, duplicate hooks
   - Update prepublishOnly to run build + test
   - Fix npx references in id3-start/SKILL.md to use id3-cli init
   ```
  </action>
  <acceptance_criteria>
    - grep 'npx' skills-global/id3-start/SKILL.md returns NO matches (zero npx references)
    - grep 'id3-cli init \.' skills-global/id3-start/SKILL.md returns matches (at least 1)
    - npm pack --dry-run 2>&1 | grep build-hooks returns NO matches
    - npm pack --dry-run 2>&1 | grep update-version returns NO matches
    - npm pack --dry-run 2>&1 | grep '\.d\.ts' returns NO matches
    - npm pack --dry-run 2>&1 | grep 'dist/templates/' returns NO matches
    - npm pack --dry-run 2>&1 | grep 'dist/bin/cli.js' returns a match
    - npm pack --dry-run 2>&1 | grep 'dist/scripts/postinstall.js' returns a match
    - npm pack --dry-run 2>&1 | grep 'templates/' returns matches
    - npm pack --dry-run 2>&1 | grep 'skills-global/' returns matches
    - git log --oneline -1 message contains "refine" or "package"
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'npx' skills-global/id3-start/SKILL.md && npm run build 2>/dev/null && npm pack --dry-run 2>&1 | grep -q 'dist/bin/cli.js' && ! npm pack --dry-run 2>&1 | grep -q 'build-hooks' && ! npm pack --dry-run 2>&1 | grep -q '\.d\.ts' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md has zero npx references, uses "id3-cli init ." instead. npm pack --dry-run shows only runtime files (no dev scripts, no .d.ts, no duplicate hooks).</done>
</task>

</tasks>

<verification>
1. node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')).repository.url" outputs "https://github.com/bruce-jsh/iddd.git"
2. node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')).engines.node" outputs ">=20.11.0"
3. grep 'npm run build && npm test' package.json matches
4. npm pack --dry-run 2>&1 | grep -c 'build-hooks\|update-version\|\.d\.ts\|dist/templates/' equals 0
5. grep -c 'npx' skills-global/id3-start/SKILL.md equals 0
6. grep 'id3-cli init \.' skills-global/id3-start/SKILL.md matches at least 1 line
</verification>

<success_criteria>
1. package.json has repository, homepage, bugs fields pointing to bruce-jsh/iddd
2. package.json engines requires Node >=20.11.0
3. files field includes only dist/bin/**/*.js, dist/src/**/*.js, dist/scripts/postinstall.js, templates/, assets/, skills-global/
4. prepublishOnly runs "npm run build && npm test"
5. SKILL.md contains zero npx references; auto-setup uses "id3-cli init ."
6. npm pack --dry-run shows no dev scripts, no .d.ts files, no dist/templates/ duplicates
7. Changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/03-repo-package-cleanup/03-02-SUMMARY.md`
</output>
